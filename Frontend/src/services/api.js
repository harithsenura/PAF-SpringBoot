// API configuration
const config = {
  API_BASE_URL: "https://paf-backend-production.up.railway.app",

  // API endpoints
  endpoints: {
    auth: "/api/auth",
    posts: "/api/posts",
    notifications: "/api/notifications",
    users: "/api/users",
  },
}

export default config
